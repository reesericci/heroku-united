<%
  def underlined?(path)
    current_page?(path) ? "text-decoration: underline" : nil
  end
%>

<div style="display: inline-flex; gap: 1rem">
  <h2><%= Config.organization %> —</h2>
  <nav style="font-size: 24px; font-weight: 400; display: inline-flex; gap: 1rem; align-items: center;">
    <%= link_to "Home", members_path, style: underlined?(members_path) %>
    <%= link_to "Broadcasts", broadcasts_path, style: underlined?(broadcasts_path) %>
    <%= link_to "API Keys", api_keys_path, style: underlined?(api_keys_path) %>
    <%= link_to "Identity", oauth_applications_path, style: underlined?(oauth_applications_path)  %>
    <%= link_to "Config", configuration_path, style: underlined?(configuration_path)  %>
  </nav>
</div>